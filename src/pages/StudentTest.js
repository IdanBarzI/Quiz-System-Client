import React from "react";
import Register from "../components/StudentTest/Register/Register";

const StudentTest = (props) => {
  return (
    <div>
      <Register />
    </div>
  );
};

export default StudentTest;
